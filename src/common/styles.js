import {StyleSheet} from 'react-native';
import {containerBgColor} from './colors';
export const commonStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: containerBgColor,
  },
});
